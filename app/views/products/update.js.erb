<% if @product.errors.empty? %>
  // add the row to the table, show alert and hide modal
  $('table tbody tr#<%= @product.id %>').replaceWith(' <%= j render "row", product: @product %>');
  $('.alert-success').html("El producto ha sido modificado con éxito").show();
  $('#product-modal').modal('hide');
<% else %>
  // show first error
  alert('<%= @product.errors.full_messages[0] %>');
<% end %>